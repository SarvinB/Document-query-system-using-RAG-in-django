version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
      - media_data:/app/media
      - faiss_data:/app/faiss_index
    ports:
      - "8000:8000"
    env_file:
      - .env

volumes:
  media_data:
  faiss_data: